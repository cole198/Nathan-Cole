After compiling using the given Makefile, the program can be run using ./"executable" -x in out
Where "executable" is the name of the executable created after compilation, x is either a or l which tells the program to sort by an array or linked list respectively, 
and in and out are input and output files. Example inputs and outputs are in binary. 
